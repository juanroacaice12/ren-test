{
  "name": "backend-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "mkdir -p dist && cp -R src/* dist/",
    "build:ui": "rmdir /s /q dist && cd ../frontend && npm run build && xcopy dist ..\\backend\\dist /E /H /C /I",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
